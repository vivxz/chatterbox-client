var Friends = {
  // $user: $('#username'),
  toggleStatus: function () {
    // toggle when clicked to know if they're a friend or not
    // var clicked = false;
    $('body').on('click', '.username', function () {
      //it works, we will finish when we get everything to wonder

      // console.log('arthur does not eat ketchup and rice');
      // clicked = true;
    })
  }
}